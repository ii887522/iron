//! It contains a bunch of top level functions that can be called directly and some extension functions applied on the
//! existing types already made in the standard library.

pub mod collections;
pub mod fs;
pub mod math;
pub mod str;
